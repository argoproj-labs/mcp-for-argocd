{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  labels:
    {{- include "mcp-for-argocd.labels" . | nindent 4 }}
type: Opaque
data:
  ARGOCD_API_TOKEN: {{ .Values.secret.argocdApiToken | b64enc }}
{{- end }}